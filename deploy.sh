# Deploy to Vercel
vercel --prod

# Or deploy to Netlify
netlify deploy --prod

# Environment variables (if needed)
# Add these in your deployment platform:
# NEXT_PUBLIC_GITHUB_TOKEN=(optional for higher rate limits)
